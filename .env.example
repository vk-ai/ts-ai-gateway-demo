# Server
PORT=3000

# LLM provider: "mock" (default, offline) or "openai"
LLM_PROVIDER=mock

# Required only when LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# RAG: max chunks to retrieve
RAG_TOP_K=3
